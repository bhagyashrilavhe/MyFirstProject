using System;
using System.Collections.Generic;
using System.Text;

namespace Test2
{
    class TwinPrimeNumber
    {
       /* static void Main(String[] args)
        {
            Console.WriteLine("Enter the start number : ");
            int num1 = int.Parse(Console.ReadLine());
            Console.WriteLine("Enter the End number : ");
            int num2 = int.Parse(Console.ReadLine());

        }
        static bool TwinPrimeNumberStart(int start ,int end)
        {
            return (start - end == 2 || end - start == 2)
                && IsPrimeNumber(start) && IsPrimeNumber(end);

        }
        static bool TwinPrimeNumberEnd(int start, int end)
        {
            return Math.Abs(start - end) == 2
             && IsPrimeNumber(start) && IsPrimeNumber(end);
        }
        static bool IsPrimeNumber(int value)
        {
            for(int i=2; i<value; i++)
            {
                if(value%i==0)
                {
                    return false;
                }
                else
                {
                    return true;
                }
            }
        }*/
    }
}
